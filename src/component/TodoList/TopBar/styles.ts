import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  topbar: {
    height: 42,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  itemCount: {fontSize: 16, fontWeight: 'bold'},
  clearBtn: {
    paddingHorizontal: 0,
    marginVertical: 0,
  },
});
